# 限定解除の所持免許判定

2026-09-11、学校回答「希望が四輪限定解除なら四輪免許、二輪限定解除なら二輪免許を対象。両方所持でも同じ」をユーザーが転送し、「これで一回直して」と修正を依頼。

## 今回の変更

- フォームの希望免許に四輪2種類（普通車AT解除、準中型5t解除）・二輪3種類（普通二輪MT、普通二輪AT、小型二輪AT解除）を追加。所持免許はすべて選択して保存する。
- GASでは限定解除を通常取得から分離し、希望と同じ系統の所持免許を参照。反対系統の免許・原付を審査区分の選択から除く。
- 正式2026-08-01 PDFの審査料金9行、学生/一般×デイ/フリーの36料金を使用。既存の通常PRICE_MASTERとMT卒業証明書移行36,300円は不変。
- 普通AT解除の4料金は62,150 / 67,150 / 67,650 / 72,650円。例えば普通AT＋普通二輪所持の一般フリーは72,650円。
- 二輪AT小型から普通MT二輪への審査は74,030 / 79,030 / 79,530 / 84,530円。四輪併有でも二輪のこの区分を使用。

## 確認待ちを維持する条件

旧フォームの「限定解除」だけの入力、四輪/二輪だけで解除先が不明、通常取得との複数希望、同じ系統に複数の審査区分、未対応免許・希望範囲取得済み、区分/プラン未選択は金額を推測しない。異なる料金だけでなく同額の別審査区分でも確認待ち。希望が通常取得の場合には、今回の限定解除規則を適用しない。

審査のオプション適用・追加割引は今回の学校回答では未確定。オプション付き審査を0円追加で確定させず確認待ちにする。既存の5件は解除種類等が未確定のため一括修復しない。顧客記録・メール・PDFの変更なし。

## 検証

- 限定解除168ケース：9審査区分の4料金、反対系統併有・原付・順序・重複、全角半角、未確定と取得済み・オプション・未設定料金。
- Worker→GAS 144往復。元の所持免許のメモリ保存、既存行の保持。
- 従来の通常料金と承認済み規則327ケース、紹介受付・既存サイト回帰。
- PC/スマホの5つの新規選択肢、両系統の所持免許選択、オプション非表示、横はみ出しなし。実申込・外部メール送信は0。

非公開原本と候補は親の `deploy-backups/20260911-limited/`。再現用GAS差分は `gas-limited-selection-20260911.patch`。本番結果は下記。


## 本番反映結果

2026-09-11 14:05 JST、既存GASデプロイを74→75へ更新成功。デプロイID・URL・実行者・アクセス設定を保持。保存後の再読み込みでCode.gs全文が検証候補と一致（SHA256 `51fb0e545a617e4ec219354f2c210db0adadcb7d7ecbfdbee509e76a64be4abc`）。原本74は同じ非公開バックアップ内。

Git main `40b2d68` を既存の安全スクリプトでCloudflareへ公開。Production deployment `dc8980a8-fb8e-4298-b36f-1852cbd4306a`。公開ドメインのPC/390pxで新しい5選択肢、両系統の所持免許保持、オプション制御、表示崩れ・JS例外なしを確認。実申込は未送信。

公開JSと計測設定はローカルと全文一致。公開HTMLは既存Cloudflareビーコンの自動挿入を除き全文一致。Pythonの直接HTTP取得は403だったため、独立したChromeで公開コンテンツを取得して照合した。今回の配備には以前のコミットで保存された計測準備コードも含まれるが、GA4は `enabled:false` / 測定ID空欄のまま無効、計測開始や権限変更はしていない。

既存申込5件・受付スプレッドシート・通知メール・PDF・D1データは変更なし。解除種類が不明な旧申込を今回の回答だけで自動確定しない。切戻し候補はGAS74とCloudflare旧production `94c796a0-e1b3-4c9f-9a8c-5199ab8f4f67`。新フォームが新GASに依存するため、切戻す場合はフォーム側を先に旧版へ戻してからGASを戻す。
